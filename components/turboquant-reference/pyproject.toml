[build-system]
requires = ["setuptools>=84.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "turboquant-reference"
version = "0.1.0"
description = "NumPy/SciPy reference implementation of TurboQuant KV-cache compression"
readme = "README.md"
license = "Apache-2.0"
license-files = ["LICENSE", "NOTICE"]
requires-python = ">=3.10,<3.15"
authors = [
    { name = "Dipesh Tharu Mahato (dipeshbabu)" },
]
keywords = [
    "llm",
    "kv-cache",
    "quantization",
    "compression",
    "polarquant",
    "turboquant",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "numpy>=1.23",
    "scipy>=1.10",
]

[project.urls]
Homepage = "https://github.com/dipeshbabu/metria/tree/main/components/turboquant-reference"
Repository = "https://github.com/dipeshbabu/metria"
Issues = "https://github.com/dipeshbabu/metria/issues"
Changelog = "https://github.com/dipeshbabu/metria/blob/main/components/turboquant-reference/CHANGELOG.md"

[project.optional-dependencies]
dev = [
    "pytest>=9.0.3",
    "pytest-cov>=4.0",
    "build>=1.0",
    "twine>=7.0.0",
]
bench = [
    "torch>=2.13",
    "transformers>=5.14.1",
    "accelerate>=0.28",
]

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]
include = ["turboquant*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
